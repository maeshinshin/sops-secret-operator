{{- if .Values.rbac.helpers.enabled }}
apiVersion: rbac.authorization.k8s.io/v1
{{- if .Values.rbac.namespaced }}
kind: Role
{{- else }}
kind: ClusterRole
{{- end }}
metadata:
{{- if .Values.rbac.namespaced }}
  namespace: {{ .Release.Namespace }}
{{- end }}
  labels:
    app.kubernetes.io/managed-by: {{ .Release.Service }}
    app.kubernetes.io/name: {{ include "sops-secret-operator.name" . }}
    helm.sh/chart: {{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}
    app.kubernetes.io/instance: {{ .Release.Name }}
  name: {{ include "sops-secret-operator.resourceName" (dict "suffix" "sopssecret-admin-role" "context" $) }}
rules:
- apiGroups:
  - sops.maesh.dev
  resources:
  - sopssecrets
  verbs:
  - '*'
- apiGroups:
  - sops.maesh.dev
  resources:
  - sopssecrets/status
  verbs:
  - get
{{- end }}
